package tv.weplay.ws.lobby.model.error;

public enum ErrorType {
    LOBBY_CANCELED,
    INVALID_LOBBY_MAP_ID,
    INVALID_USER_ID,
    INVALID_CARD_ID,
    LOBBY_NOT_EXIST,
    ACTION_AFTER_LOBBY_ENDED

}
